# 实验 3 同步与基线测试记录

## Git 同步

- 远端：`https://github.com/lyc-0428/robo_ex3.git`
- 分支：`main`
- 本地和远端提交：`e24495e8e68033692dbd75821f12c9115bb6e5df`
- 提交时间：`2026-09-13T13:18:15+08:00`
- ahead：0
- behind：0
- push：未执行
- 原工程跟踪文件：未修改

## 原工程测试

命令：

```powershell
python -m unittest discover -s digital\tests -v
```

结果：21 项中 20 项通过、1 项失败。

失败项：`test_vision_common.VisionCommonTests.test_generated_object_sdf_is_valid_xml`。

原因：测试仍要求 `water_bottle_02/meshes/bottle_body.obj` 和 `bottle_cap.obj`，运行实现已改为读取 `water_bottle_03/model.sdf` 与 `bottle_complete.obj`。这是测试与当前模型版本不一致。

## try 改进版测试

命令：

```powershell
python -m unittest discover -s try\tests -v
```

最终结果：22 项全部通过，覆盖标准检测接口、持久化识别日志、显式状态机、可配置类别配额和世界位姿读取。

## 本地正式应用记录

- 推荐覆盖层已于 2026-09-13 应用到本地 `digital`。
- 原测试已同步到六物体随机布局、正六棱柱/立方体碰撞模型和可配置类别配额。
- 新增闭环位姿控制测试后，`digital/tests` 共 25 项全部通过。
- `try/tests` 22 项全部通过。
- 未执行 `git add`、`git commit` 或 `git push`。
