# Detection weight

The runtime copy of the repository weight
`model/bottle_tennisball_best.pt` is stored as `best.pt` in this directory.
The runtime intentionally refuses to start when the file is absent or when its
embedded class names do not contain both the bottle and tennis-ball task classes.
