# Active version: tennis test stable

This branch preserves the stable tennis-only placement test profile.

Run it on the Jetson with:

```bash
cd /home/adam/Team21/lyc/robo_ex3/digital
bash run_tennis_placement_debug.sh 21
```

The bottle sorting profile is kept in the repository as a separate, reusable
profile, but it is not the validation target of this branch.
